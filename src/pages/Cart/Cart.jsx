import { CartFeature } from '../../features/cart';

const Cart = () => {
    return (
        <main>
            <section className="container mx-auto px-4 py-8">
                <CartFeature />
            </section>
        </main>
    );
};

export default Cart;
